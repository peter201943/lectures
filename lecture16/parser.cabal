name:          parser
version:       0.1
cabal-version: >= 1.10

build-type: Simple

library
  build-depends:
    base >= 4 && < 5

  exposed-modules:
    Exp
    Parser

  default-language: Haskell2010

  ghc-options: -Wall
