name:          testing
version:       0.1
cabal-version: >= 1.10

build-type: Simple

library
  build-depends:
    base       >= 4 && < 5,
    hspec      >= 2.5  && < 2.8,
    QuickCheck >= 2.11 && < 2.14

  exposed-modules:
    Insert

  default-language: Haskell2010

  ghc-options: -Wall
